DROP TABLE RESTAURANT_HIST IF EXISTS;
CREATE TABLE RESTAURANT_HIST (LICENSENO VARCHAR(10) NOT NULL, ADDRESS VARCHAR(65535), DISTRICTCODE SMALLINT, INFOCODE VARCHAR(16), NAME VARCHAR(256), TYPECODE VARCHAR(2), VERSION INTEGER, START_DATE TIMESTAMP, END_DATE TIMESTAMP, PRIMARY KEY (LICENSENO, START_DATE));